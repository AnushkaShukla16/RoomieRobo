def resolve_conflicts(member_name: str) -> str:
    """
    Resolves all chore assignment conflicts for a given household member.

    Args:
        member_name (str): Name of the person whose conflicts are being resolved.

    Returns:
        str: A confirmation message.
    """
    return f"Resolved all conflicts for {member_name}."

